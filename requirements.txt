# M1 — single agent + search tool
openai>=1.40.0          # Groq speaks the OpenAI API
tavily-python>=0.5.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.0

# M2 — orchestration
langgraph>=0.2.0

# M5 — UI
streamlit>=1.37.0
